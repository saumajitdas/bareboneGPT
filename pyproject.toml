[project]
name = "barebonellm"
version = "0.1.0"
description = "Barebone GPT-style LLM from scratch (train + generate + serve)."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
